import { WeeklyScreen } from "@/features/weekly/weekly-screen";

/**
 * Purpose: Render weekly planner route.
 * Inputs: URL search params consumed by client screen.
 * Output: Weekly feature screen.
 * Side effects: None.
 */
export default function WeeklyPage() {
  return <WeeklyScreen />;
}
