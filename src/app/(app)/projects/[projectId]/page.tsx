import { ProjectDetailScreen } from "@/features/projects/project-detail-screen";

/**
 * Purpose: Render project detail route.
 * Inputs: Dynamic project ID consumed by client screen.
 * Output: Project detail feature screen.
 * Side effects: None.
 */
export default function ProjectDetailPage() {
  return <ProjectDetailScreen />;
}
